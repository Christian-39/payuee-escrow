# Django Core
django>=5.0,<6.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0

# Database
# Only PyMySQL is needed - settings.py calls pymysql.install_as_MySQLdb()
# to use it as the MySQLdb driver. psycopg2-binary (Postgres) and
# mysqlclient (a second, competing MySQL driver) were leftover from an
# earlier Postgres-> MySQL migration and were never actually used.
PyMySQL==1.1.2

# Authentication
djangorestframework-simplejwt>=5.3.0
python-jose>=3.3.0

# Configuration
python-decouple>=3.8
python-dotenv==1.2.2

# Storage
boto3>=1.34.0
django-storages>=1.14.0
Pillow>=10.1.0

# API & HTTP
requests>=2.31.0

# Task Queue
celery>=5.3.0
redis>=5.0.0

# Filtering
django-filter>=23.5

# Production
gunicorn>=21.2.0
whitenoise>=6.6.0